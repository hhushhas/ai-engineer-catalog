# MCPs are Boring (or: Why we are losing the Sparkle of LLMs)

**Speaker:** Manuel Odendahl
**Source:** https://www.youtube.com/watch?v=J3oJqan2Gv8
**Duration:** 28:32
**Recommendation:** Optional
**Hasan-fit themes:** general ai engineering

## Gist

The assistant can do some chain of thought for example and uh checks what tools it has available which are provided as a schema in the context that's being sent to the LLM.

## Why Hasan Should Care

This session maps to general ai engineering. For Hasan's work, the useful lens is whether it changes how to build, evaluate, ship, or operate AI-enabled products and agent workflows (28:32).

## Practical Takeaways

- Um, but great, we have MCP now, which allows any kind of LLM app to interact with tools that other people have built.
- So if we if we want to call the tool and say like you know we have this like fragment of document please search for something similar somewhere else you have to emit this part of document which probably is already in the context higher above anyway.
- So we can engineer around it but really what we're doing is uh why are MCPs basically so restricted and I think it's because they're boring like we are not leveraging all the things we could actually do with LLM with code generated by LM with the coding the tool calling around it for example why don't we pass the chat history along with LLM calls the LM call can decide to the tool can just decide to to discard it right but we already have all this information in the context of an application why not also pass it to the tool call so that we can say you know we've called this thing like 15 times just reuse the arguments from before for example or use this as you know a search query um so that would be one example it's pretty pretty simple it's like you know please just give me the the chat history that is present as metadata to your tool call you could also give me your memories.
- If you have an LM application that has memories and you have persistent knowledge like why not give that to my tool to say like well you've already called me 15 times like maybe my location is San Francisco then I can just call get weather and get weather will look at my memories and say like oh there's a location memory here you go um pretty easy extension that allows you to do so so so much more at a very very low cost and another thing is like if you can attach files in your LLM application like why not pass these files along or at least a path or like at least their metadata dangerous right like the security implications and the problem is also there's many many many different NLM apps that all have different modalities so suddenly you have to design a protocol that covers all these different cases maybe I have an application where people can like draw little images so suddenly how do I pass that as as an attached file like maybe I have a graph rag how do I pass my graph structure Lots of questions, lot of boring engineering.
- Um so to show you an example of what that can look like is um you can say I want to find all the customers with overdue invoices, right?

## Implementation Ideas

- Use this as background signal unless the title/speaker is directly relevant to an active project.
